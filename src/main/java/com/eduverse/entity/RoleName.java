package com.eduverse.entity;

/**
 * ============================================================================
 * ROLE NAME ENUM
 * ============================================================================
 * 
 * An Enum is a special "class" in Java that represents a group of constants
 * (unchangeable variables, like final variables). 
 * 
 * By defining our roles (STUDENT, INSTRUCTOR, ADMIN) in an enum, we prevent any
 * spelling errors during coding and ensure compile-time safety. 
 */
public enum RoleName {
    STUDENT,
    INSTRUCTOR,
    ADMIN
}
